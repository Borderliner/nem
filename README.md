# nem

A terminal text editor with nano's shape and emacs's keybindings.

Buffers, window splits, prefix keymaps, `M-x`, incremental search, kill ring,
mark and region, linear undo/redo. Configured and scripted in Lua.

Status: in development.
